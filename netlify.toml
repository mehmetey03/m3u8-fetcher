[build]
  command = "npm install"
  functions = "api"
  publish = "public"

[[redirects]]
  from = "/fetch/*"
  to = "/.netlify/functions/fetch/:splat"
  status = 200

[build.environment]
  PUPPETEER_SKIP_CHROMIUM_DOWNLOAD = "true"
