{
  "name": "m3u8-fetcher",
  "version": "1.0.0",
  "main": "api/fetch.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "puppeteer": "^21.7.0",
    "node-fetch": "^3.3.2",
    "@netlify/functions": "^1.4.0"
  },
  "engines": {
    "node": ">=18.x"
  }
}
